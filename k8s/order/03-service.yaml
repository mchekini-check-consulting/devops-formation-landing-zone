apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: dev
  labels:
    app: order
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
    - name: http
      port: 8000
      targetPort: 8000
