apiVersion: v1
kind: Service
metadata:
  name: pgpool
  namespace: dev
  labels:
    app: pgpool
spec:
  type: ClusterIP
  selector:
    app: pgpool
  ports:
    - port: 5432
      targetPort: 9999
      protocol: TCP
